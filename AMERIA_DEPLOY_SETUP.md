# Ameria Bank Integration - Deploy Setup Guide

## 🚀 Подготовка к Deploy

### ✅ Что уже готово:

1. **Код полностью исправлен** согласно спецификации
2. **Callback URL автоматически определяется** через `getBaseUrl()` утилиту
3. **Admin панель** автоматически устанавливает правильный callback URL

---

## 📋 Шаги для Deploy

### 1. Environment Variables (Переменные окружения)

Добавьте в ваш `.env` или в настройки deploy платформы (Vercel, etc.):

```bash
# Base URL для production (опционально, но рекомендуется)
NEXT_PUBLIC_APP_URL=https://yourdomain.com

# Encryption key для шифрования credentials (ОБЯЗАТЕЛЬНО!)
PAYMENT_ENCRYPTION_KEY=your-secure-32-character-key-here!!

# Или используйте существующий
ENCRYPTION_KEY=your-secure-32-character-key-here!!
```

**Важно:** `PAYMENT_ENCRYPTION_KEY` должен быть **минимум 32 символа** для AES-256-CBC шифрования.

---

### 2. Автоматическое определение URL

Система автоматически определяет base URL в следующем порядке:

1. **`NEXT_PUBLIC_APP_URL`** (если установлен) - для production
2. **`VERCEL_URL`** (если deploy на Vercel) - автоматически
3. **`window.location.origin`** (client-side) - автоматически после deploy
4. **`localhost:3000`** (fallback) - только для development

**После deploy:** Callback URL будет автоматически правильным благодаря `window.location.origin`.

---

### 3. Настройка в Admin панели

После deploy:

1. Зайдите в `/admin/payments`
2. Заполните credentials от Ameria Bank:
   - **Client ID**
   - **Username**
   - **Password**
3. **Callback URL** будет автоматически установлен на правильный URL
4. Проверьте, что **Return URL** = **Callback URL** = `https://yourdomain.com/api/v1/payments/ameria/callback`
5. Выберите **Test Mode** или **Live Mode**
6. Нажмите **"Validate & Activate"**

---

### 4. Проверка Callback URL

**Правильный формат:**
```
https://yourdomain.com/api/v1/payments/ameria/callback
```

**НЕ используйте:**
- ❌ `http://localhost:3000/...` (только для local development)
- ❌ `https://yourdomain.com/orders/[ORDER_NUMBER]` (это не callback URL)

---

## 🔒 Безопасность

### Encryption Key

**КРИТИЧНО:** Убедитесь, что `PAYMENT_ENCRYPTION_KEY` установлен в production:

```bash
# Generate secure key (32+ characters)
openssl rand -base64 32
```

Или используйте любой secure random string минимум 32 символа.

**Никогда не коммитьте** encryption key в git!

---

## 🧪 Тестирование после Deploy

### 1. Проверка конфигурации

После deploy и настройки credentials:

1. Зайдите в `/admin/payments`
2. Нажмите **"Validate & Activate"**
3. Должно появиться: "Connection successful. Credentials are valid."

### 2. Тестовый платеж

1. Создайте тестовый заказ
2. Выберите **Ameria Bank** как способ оплаты
3. Завершите checkout
4. Должно произойти перенаправление на страницу Ameria Bank
5. После оплаты (или отмены) вы вернетесь на callback URL
6. Заказ должен обновиться со статусом оплаты

---

## 📝 Важные моменты

### Callback URL работает автоматически

✅ **После deploy:**
- Callback URL автоматически будет правильным
- Используется `window.location.origin` в браузере
- Не нужно вручную менять URL после каждого deploy

### Test Mode vs Production Mode

- **Test Mode:** Использует `https://servicestest.ameriabank.am/VPOS`
- **Production Mode:** Использует `https://services.ameriabank.am/VPOS`

Переключение происходит через admin панель.

### Ameria Bank не требует регистрации домена

✅ Можно использовать любой домен:
- Production domain: `https://yourdomain.com`
- Vercel preview: `https://yourproject.vercel.app`
- Custom domain: `https://shop.yourdomain.com`

---

## 🐛 Troubleshooting

### Проблема: Callback URL неправильный

**Решение:**
1. Проверьте, что в admin панели Callback URL = `https://yourdomain.com/api/v1/payments/ameria/callback`
2. Если неправильный, просто сохраните конфигурацию заново - URL обновится автоматически

### Проблема: "Payment initialization failed"

**Проверьте:**
1. Credentials правильные (Client ID, Username, Password)
2. Test Mode включен для тестирования
3. Callback URL доступен из интернета (не localhost)

### Проблема: "Connection test failed"

**Проверьте:**
1. Credentials от Ameria Bank правильные
2. Интернет соединение работает
3. Test Mode включен (для тестовых credentials)

---

## ✅ Checklist перед Deploy

- [ ] `PAYMENT_ENCRYPTION_KEY` установлен в environment variables
- [ ] `NEXT_PUBLIC_APP_URL` установлен (опционально, но рекомендуется)
- [ ] Credentials от Ameria Bank получены
- [ ] Admin панель доступна после deploy
- [ ] Callback URL проверен в admin панели
- [ ] Test Mode включен для первого теста
- [ ] Готовы к тестированию payment flow

---

## 🎯 После Deploy

1. **Настройте credentials** в `/admin/payments`
2. **Validate & Activate** для проверки соединения
3. **Протестируйте** с тестовым заказом
4. **Переключите на Production Mode** когда готовы к реальным платежам

---

**Готово!** 🎉

Все настроено для автоматической работы после deploy. Callback URL будет правильным автоматически благодаря `getBaseUrl()` утилите.


